<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
<comment>member-sql-phj.xml</comment>

	<!-- 비밀번호 조회 -->
	<entry key="selectPw">
		SELECT MEMBER_PW, MEMBER_Q_CD, MEMBER_A
		FROM MEMBER
		WHERE MEMBER_ID = ?
	</entry>
	
	<!-- 비밀번호 수정 -->
	<entry key="updatePw">
		UPDATE MEMBER SET
		MEMBER_PW = ?
		WHERE MEMBER_ID = ?
	</entry>
	
	<!-- 회원 게시글 목록 조회 -->
	<entry key="selectBoard">
		SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, MEMBER_NO, BOARD_CD
		FROM BOARD
		WHERE MEMBER_NO = ?
	</entry>
	
	<!-- 게시글 삭제  -->
	<entry key="deleteBoard">
		UPDATE BOARD SET
		BOARD_ST = 'Y'
		WHERE BOARD_NO = ?
	</entry>
	
	


</properties>
